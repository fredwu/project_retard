p= f.input :email
p= f.input :first_name
p= f.input :last_name
p= f.input :deal_city, :as => :select, :collection => User::CITIES, :prompt => true
br
p= f.input :email_notification
br

- if defined?(show_password)
  p= f.input :password
  p= f.input :password_confirmation

p.input= f.button :submit, :class => "submit mid"
